# Report hero asset

The image report uses the built-in CSS mascot by default.

To use a custom authorized illustration, place a PNG at:

`assets/report_hero.png`

Recommended size: 900 x 760 pixels. Keep the subject near the center/right and avoid
important details along the bottom edge because the report header masks that area.
